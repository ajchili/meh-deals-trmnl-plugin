services:
  trmnlp:
    image: schrockwell/trmnlp
    ports:
      - 4567:4567
    volumes:
      - .:/plugin
    restart: unless-stopped
  server:
    build: .
    command: "uv run fastapi dev src/main.py --host 0.0.0.0"
    ports:
      - 8000:8000
    volumes:
      - .:/app
